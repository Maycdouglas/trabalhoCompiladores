abstract data Racional {
  numerador :: Int;
  denominador :: Int;
  
  soma(r :: Racional) : Racional {
    Racional res = new Racional;
    res.numerador = /* cálculo */;
    res.denominador = /* cálculo */;
    return res;
  }
}

main() {
  print fat(10)[0];
}

fat(n :: Int) : Int {
  if (n < 1) return 1;
  else return n * fat(n-1)[0];
}